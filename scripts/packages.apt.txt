# Packages commonly expected by these dotfiles and submodules (Ubuntu/Debian).
# Install manually or let INSTALL.sh / require_dep pull them as needed.
curl
git
xsel
sxhkd
xfconf
xdotool
# Optional / environment-dependent:
# alacritty kitty tmux neovim
